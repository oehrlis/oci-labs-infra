# Jumphost templates

Cloud-init templates used by the jumphost/gateway module. `jumphost-cloudinit.yaml.tftpl` configures SSH hardening, firewall rules, fail2ban, and optional WireGuard package installation. Update these templates if you need additional bootstrap actions or customizations for the jumphost.
